# 特征值检查报告

## 检查信息
- **apply_id**: 1065479921833549825
- **时间**: 2025-11-24 21:45:47.808
- **特征**: cdc_consultas_30d_shop_daily_cnt_mean_v2
- **实际值**: 0.0
- **窗口**: 30 天

## 检查结果

### 1. 原始数据情况
- ✅ 找到目标记录
- ✅ response_body 正常（非空，合法 JSON）
- ✅ consultas 字段存在（共 9 条记录）

### 2. 窗口筛选结果
**30 天窗口内的记录数**: 1 条

**窗口内记录明细**:
| fechaConsulta | nombreOtorgante | otorgante_group | days_before_request |
|---------------|-----------------|-----------------|---------------------|
| 2025-11-24 | CAMELINAS MECANICAS | 其他 | 0.906803 |

**关键发现**:
- 窗口内有 1 条查询记录
- 但这条记录的机构类别是 **"其他"**（CAMELINAS MECANICAS 不在 17 个大类中）
- **窗口内没有 "商店信息"（shop）类别的记录**

### 3. 特征计算逻辑分析

根据第一板块衍生脚本的代码逻辑（第839行）:

```python
if len(sub_cat_day) == 0:
    daily_mean = pd.DataFrame(-999, index=df_base.index, columns=groups_to_use)
    daily_std = pd.DataFrame(-999, index=df_base.index, columns=groups_to_use)
```

**当窗口内没有该类别（shop）的查询记录时**:
- 应该填充 **-999**

### 4. 结论

**特征值 0.0 是填充值，不是计算值**

**原因分析**:
1. 窗口内没有 shop 类别的查询记录
2. 按照当前代码逻辑，应该填充 **-999**
3. 但实际值是 **0.0**

**这说明**:
- ✅ **数据是用旧版本代码生成的**
- 旧版本代码在窗口内没有该类别记录时，填充的是 **0.0**
- 新版本代码已经修改为填充 **-999**

### 5. 代码版本对比

**旧版本代码**（填充 0.0）:
```python
if len(sub_cat_day) == 0:
    daily_mean = pd.DataFrame(0.0, index=df_base.index, columns=groups_to_use)
    daily_std = pd.DataFrame(0.0, index=df_base.index, columns=groups_to_use)
```

**新版本代码**（填充 -999）:
```python
if len(sub_cat_day) == 0:
    daily_mean = pd.DataFrame(-999, index=df_base.index, columns=groups_to_use)
    daily_std = pd.DataFrame(-999, index=df_base.index, columns=groups_to_use)
```

### 6. 建议

如果需要使用新的填充规则（-999），需要：
1. 使用新版本代码重新运行第一板块衍生脚本
2. 重新生成特征数据

## 总结

对于 apply_id **1065479921833549825**:
- 特征 `cdc_consultas_30d_shop_daily_cnt_mean_v2` 的值 **0.0** 是**填充值**
- 这是因为 30 天窗口内没有 shop 类别的查询记录
- 使用的是旧版本代码（填充 0.0）
- 如果使用新版本代码，该值应该是 **-999**

---
生成时间: 2026-01-30
