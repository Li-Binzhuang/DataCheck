# CDC板块衍生脚本 - 分批输出更新状态检查

## 检查时间
2026-01-28

## 检查结果

### ❌ 所有四个板块都还没有更新为分批输出

| 板块 | 当前状态 | 需要更新 |
|------|----------|----------|
| 第一板块衍生.ipynb | ❌ 使用 WRITE_SAMPLE_200 | ✅ 是 |
| 第二板块衍生.ipynb | ❌ 使用 WRITE_SAMPLE_200 | ✅ 是 |
| 第三板块衍生.ipynb | ❌ 使用 WRITE_SAMPLE_200 | ✅ 是 |
| BOSS板块衍生.ipynb | ❌ 使用 WRITE_SAMPLE_200 | ✅ 是 |

## 当前问题

### 所有板块都在使用旧的输出方式：
1. 输出全量文件（如 `cdc1_features_consultas.csv`）
2. 输出sample200文件（如 `cdc1_features_consultas_sample200.csv`）

### 需要改为：
1. 删除全量文件和sample200文件输出
2. 改为分批输出，每个文件500条数据
3. 文件命名：`{前缀}_batch{批次号:03d}_{起始行}-{结束行}.csv`

## 修改方案

### 需要修改的内容

#### 1. 配置部分
**删除**：
```python
WRITE_SAMPLE_200 = True  # ❌ 删除这行
```

**添加**：
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # ✅ 新增：每个文件的行数
```

#### 2. 输出逻辑部分
**删除**：
- 全量文件输出：`_features_to_write.to_csv(features_out_path, ...)`
- sample200输出：`if WRITE_SAMPLE_200: ...`

**替换为**：分批输出循环（见详细文档）

## 详细修改指南

详见：`docs/分批输出/分批输出功能说明.md`

## 修改优先级

建议按以下顺序修改：

1. **第一板块** - 最常用，优先修改
2. **BOSS板块** - 数据量大，需要分批
3. **第二板块** - 中等优先级
4. **第三板块** - 中等优先级

## 修改步骤

### 1. 打开Jupyter Notebook
在Jupyter中打开对应的板块衍生脚本

### 2. 找到输出部分
搜索 `WRITE_SAMPLE_200` 或 `WRITE_FEATURES_CSV`

### 3. 修改配置
- 删除 `WRITE_SAMPLE_200` 行
- 添加 `BATCH_SIZE = 500` 行

### 4. 修改输出逻辑
按照 `docs/分批输出/分批输出功能说明.md` 中的代码模板修改

### 5. 测试运行
运行脚本，检查输出文件是否正确

## 预期输出

### 修改后的输出文件示例

#### 第一板块（假设1500行数据）
```
CDC/outputs/cdc1_features_batch001_1-500.csv
CDC/outputs/cdc1_features_batch002_501-1000.csv
CDC/outputs/cdc1_features_batch003_1001-1500.csv
```

#### 第二板块（假设800行数据）
```
CDC/outputs/cdc2_features_batch001_1-500.csv
CDC/outputs/cdc2_features_batch002_501-800.csv
```

#### 第三板块（假设600行数据）
```
CDC/outputs/cdc3_features_batch001_1-500.csv
CDC/outputs/cdc3_features_batch002_501-600.csv
```

#### BOSS板块（假设2000行数据）
```
CDC/outputs/cdcboss_features_batch001_1-500.csv
CDC/outputs/cdcboss_features_batch002_501-1000.csv
CDC/outputs/cdcboss_features_batch003_1001-1500.csv
CDC/outputs/cdcboss_features_batch004_1501-2000.csv
```

## 相关文档

- 详细修改说明：`docs/分批输出/分批输出功能说明.md`
- 快速参考：`docs/分批输出/分批输出快速参考.md`
- 代码模板：`scripts/update_scripts/add_sample200_output.py`

## 下一步行动

1. ✅ 阅读详细修改说明
2. ⏳ 修改第一板块
3. ⏳ 修改BOSS板块
4. ⏳ 修改第二板块
5. ⏳ 修改第三板块
6. ⏳ 测试所有板块的输出

---

**检查时间**：2026-01-28  
**检查结果**：❌ 所有板块都需要更新  
**修改状态**：⏳ 待修改
