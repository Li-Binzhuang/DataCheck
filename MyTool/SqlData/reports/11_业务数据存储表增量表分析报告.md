# 业务数据存储表增量表分析报告

## 一、表基本信息

**表名**: `ods_mx_allinone_t_kepler_scene_metric_data_df` (业务数据存储表增量表)  
**表代码**: ods_mx_allinone_t_kepler_scene_metric_data_df  
**总记录数**: 2条  
**字段数**: 14个

## 二、表结构分析

### 2.1 个人分析

**表的作用定位**：
业务数据存储表增量表存储业务场景的指标数据，用于数据分析和监控。

### 2.2 核心字段
- `id`: 主键ID
- `seq_id`: 序列ID
- `cust_no`: 客户编号
- `apply_id`: 申请ID
- `apply_type`: 申请类型
- `scene_code`: 场景代码
- `metric_type`: 指标类型
- `request_parm`: 请求参数
- `response_info`: 响应信息
- `res_code`: 响应代码
- `is_delete`: 是否删除

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `seq_id` | 序列ID |
| `cust_no` | 客户编号 |
| `apply_id` | 申请ID |
| `apply_type` | 申请类型 |
| `scene_code` | 场景代码 |
| `metric_type` | 指标类型 |
| `request_parm` | 请求参数 |
| `response_info` | 响应信息 |
| `res_code` | 响应代码 |
| `is_delete` | 是否删除 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |
| `pt` | 分区日期 |

</details>

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `seq_id` | 序列ID |
| `cust_no` | 客户编号 |
| `apply_id` | 申请ID |
| `apply_type` | 申请类型 |
| `scene_code` | 场景代码 |
| `metric_type` | 指标类型 |
| `request_parm` | 请求参数 |
| `response_info` | 响应信息 |
| `res_code` | 响应代码 |
| `is_delete` | 是否删除 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |
| `pt` | 分区日期 |

</details>

### 2.3 关键字段统计
- **id**: 非空值2个，唯一值2个
- **cust_no**: 非空值2个，唯一值1个
- **apply_id**: 非空值2个，唯一值2个
- **is_delete**: 非空值2个，唯一值2个

## 三、数据结果分析

### 3.1 删除标志 (is_delete) 分析
- **字段值异常**: 存储了JSON格式数据，不是标准的0/1值
- **需要解析**: 需要解析JSON数据获取实际的删除标志

### 3.2 场景代码分析
- **scene_code**: 需要进一步统计
- **metric_type**: 需要进一步统计

### 3.3 申请类型分析
- **apply_type**: 需要进一步统计

## 四、表关联关系

### 4.1 关联字段
- **apply_id**: 可能关联到授信申请表或其他申请表的id
- **cust_no**: 关联到其他表的客户编号字段

### 4.2 关联关系
- **→ 授信申请表**: 可能通过apply_id关联
- **→ 其他业务表**: 通过cust_no关联

### 4.3 关联分析
- 数据量较小（仅2条），关联关系不明显
- 主要用于存储业务场景的指标数据

## 五、业务流程位置

```
业务场景（各种业务操作）
  ↓
业务数据存储表增量表（本表：存储指标数据）
  ↓
（数据分析）
```

## 六、关键业务指标

### 6.1 数据量
- **总记录数**: 2条
- **数据量较小**: 无法进行深入分析

### 6.2 场景覆盖
- 需要进一步统计分析

## 七、数据质量评估

- ⚠️ 数据量太小，无法进行有效分析
- ⚠️ is_delete字段值异常，需要规范化
- ⚠️ 需要明确表的业务用途和数据格式

## 八、业务建议

1. **数据规范化**: 规范化is_delete字段的值
2. **扩大数据量**: 增加更多数据样本
3. **明确用途**: 明确表的业务用途和数据格式

---

**报告生成时间**: 2025-12-17






