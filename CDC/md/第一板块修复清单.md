# 第一板块衍生脚本修复清单

## 📋 需要修复的位置（6处）

### 位置1-3: 机构大类特征（无数据时）

**代码位置**: 约第 413-419 行

**当前代码**:
```python
out[f"consultas_{window_days}d_{gid}_cnt"] = 0  # 该类数量：0
out[f"consultas_{window_days}d_{gid}_ratio"] = 0.0  # 该类占比：0
out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = 0.0  # notNull 占比：0（没有记录）
```

**修改为**:
```python
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_{gid}_cnt"] = -999  # 该类数量：无数据
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_{gid}_ratio"] = -999  # 该类占比：无数据
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = -999  # notNull 占比：无数据
```

**上下文**:
```python
# 在这个 else 分支中（当窗口内没有该类型的记录时）
else:
    # 没有该类型的记录
    out[f"consultas_{window_days}d_{gid}_cnt"] = 0  # ← 修改这里
    out[f"consultas_{window_days}d_{gid}_ratio"] = 0.0  # ← 修改这里
    # ...
    out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = 0.0  # ← 修改这里
```

---

### 位置4-6: tipoCredito特征（无数据时）

**代码位置**: 约第 501-507 行

**当前代码**:
```python
out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = 0
out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = 0.0
out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = 0.0
```

**修改为**:
```python
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = -999
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = -999
# zlf update: 无数据时填充-999
out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = -999
```

**上下文**:
```python
# 在这个 else 分支中（当窗口内没有该 tipoCredito 的记录时）
else:
    # 没有该类型的记录
    out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = 0  # ← 修改这里
    out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = 0.0  # ← 修改这里
    # ...
    out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = 0.0  # ← 修改这里
```

---

## 🔍 如何定位这些代码

### 方法1: 使用行号搜索

在 Jupyter Notebook 中：
1. 打开 `CDC/第一板块衍生.ipynb`
2. 使用 Ctrl+F 搜索以下内容

### 方法2: 搜索特征名

搜索以下字符串：

**位置1-3**:
```
consultas_{window_days}d_{gid}_cnt"] = 0
```

**位置4-6**:
```
consultas_{window_days}d_tipo_{tid}_cnt"] = 0
```

### 方法3: 搜索注释

搜索以下注释：
```
# 该类数量：0
# 该类占比：0
# notNull 占比：0（没有记录）
```

---

## 📝 完整的代码块示例

### 示例1: 机构大类特征（第413-419行附近）

**修改前**:
```python
else:
    # 没有该类型的记录
    out[f"consultas_{window_days}d_{gid}_cnt"] = 0  # 该类数量：0
    out[f"consultas_{window_days}d_{gid}_ratio"] = 0.0  # 该类占比：0
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_{gid}_days_mean"] = -999  # 均值：无数据用 -999
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_{gid}_days_std"] = -999  # 标准差：无数据用 -999
    out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = 0.0  # notNull 占比：0（没有记录）
```

**修改后**:
```python
else:
    # 没有该类型的记录
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_{gid}_cnt"] = -999  # 该类数量：无数据
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_{gid}_ratio"] = -999  # 该类占比：无数据
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_{gid}_days_mean"] = -999  # 均值：无数据用 -999
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_{gid}_days_std"] = -999  # 标准差：无数据用 -999
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = -999  # notNull 占比：无数据
```

### 示例2: tipoCredito特征（第501-507行附近）

**修改前**:
```python
else:
    # 没有该类型的记录
    out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = 0
    out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = 0.0
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_days_mean"] = -999
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_days_std"] = -999
    out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = 0.0
```

**修改后**:
```python
else:
    # 没有该类型的记录
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = -999
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = -999
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_days_mean"] = -999
    # zlf update: 特征值为空时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_days_std"] = -999
    # zlf update: 无数据时填充-999
    out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = -999
```

---

## ✅ 修复检查清单

修改完成后，检查以下内容：

- [ ] 第 413 行: `consultas_{window_days}d_{gid}_cnt` 改为 -999
- [ ] 第 414 行: `consultas_{window_days}d_{gid}_ratio` 改为 -999
- [ ] 第 419 行: `consultas_{window_days}d_{gid}_notnull_ratio` 改为 -999
- [ ] 第 501 行: `consultas_{window_days}d_tipo_{tid}_cnt` 改为 -999
- [ ] 第 502 行: `consultas_{window_days}d_tipo_{tid}_ratio` 改为 -999
- [ ] 第 507 行: `consultas_{window_days}d_tipo_{tid}_notnull_ratio` 改为 -999
- [ ] 每处修改都添加了 `# zlf update: 无数据时填充-999` 注释

---

## 🔄 验证方法

修改完成后，运行检查脚本：

```bash
python CDC/scripts/check_and_fix_fillna.py
```

期望结果：
```
第一板块:
  = 0: 0 处 ✅
```

---

## 📊 影响的特征

修改后，以下特征在无数据时将输出 -999 而不是 0：

### 机构大类特征（17个大类 × 3个窗口 × 3个特征）
- `cdc_consultas_30d_shop_cnt_v2`
- `cdc_consultas_30d_shop_ratio_v2`
- `cdc_consultas_30d_shop_notnull_ratio_v2`
- `cdc_consultas_30d_bank_cnt_v2`
- `cdc_consultas_30d_bank_ratio_v2`
- `cdc_consultas_30d_bank_notnull_ratio_v2`
- ... (其他15个大类)

### tipoCredito特征（3个类型 × 3个窗口 × 3个特征）
- `cdc_consultas_30d_tipo_cc_cnt_v2`
- `cdc_consultas_30d_tipo_cc_ratio_v2`
- `cdc_consultas_30d_tipo_cc_notnull_ratio_v2`
- `cdc_consultas_30d_tipo_pp_cnt_v2`
- `cdc_consultas_30d_tipo_pp_ratio_v2`
- `cdc_consultas_30d_tipo_pp_notnull_ratio_v2`
- `cdc_consultas_30d_tipo_tc_cnt_v2`
- `cdc_consultas_30d_tipo_tc_ratio_v2`
- `cdc_consultas_30d_tipo_tc_notnull_ratio_v2`

**总计**: 约 153 个特征（17×3×3 + 3×3×3 = 153）

---

**创建时间**: 2025-01-29  
**需要修复**: 6 处  
**预计时间**: 5-10 分钟
