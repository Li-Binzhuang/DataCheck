# 跑数输入目录

将需要跑数的CSV文件放在此目录下，或通过Web界面上传。

## CSV文件格式要求

- 第一行为表头
- 从第二行开始为数据
- 根据入参配置，指定列作为接口入参

## 示例

假设CSV文件有以下列：
- A列(索引0): apply_id
- B列(索引1): cust_no  
- C列(索引2): create_time

入参配置示例：
- custNo -> 列索引1 (B列)
- baseTime -> 列索引2 (C列), 勾选"时间字段"
