# 账户标签表分析报告

## 一、表基本信息

**表名**: `ods_mx_cust_cust_account_tag_df` (账户标签表)  
**表代码**: ods_mx_cust_cust_account_tag_df  
**总记录数**: 1条  
**字段数**: 10个

## 二、表结构分析

### 2.1 个人分析

**表的作用定位**：
账户标签表是客户画像体系的基础表，用于存储客户的各类标签信息。这些标签主要用于风险识别、客户分类和业务规则决策。
    1.tag_key全部为信用模型
    2.tag_value：客户信用模型有三种：S X R
    3.客户信用模型有三种的含义是什么？
    猜测：S可能代表优质客户，X代表高风险，R代表需要审查

### 2.2 核心字段
- `id`: 主键ID
- `cust_no`: 客户编号
- `tag_key`: 标签键(CREDIT_MODEL 信用模型)
- `tag_value`: 标签值(三种值：S X R )
- `is_delete`: 是否删除
- `create_time`: 创建时间
- `update_time`: 更新时间

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `cust_no` | 客户编号 |
| `tag_key` | 标签键 |
| `tag_value` | 标签值 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |
| `create_id` | 创建人ID |
| `update_id` | 更新人ID |
| `is_delete` | 是否删除 |
| `pt` | 分区日期 |

</details>

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `cust_no` | 客户编号 |
| `tag_key` | 标签键 |
| `tag_value` | 标签值 |
| `create_time` | 创建时间 |
| `update_time` | 更新时间 |
| `create_id` | 创建人ID |
| `update_id` | 更新人ID |
| `is_delete` | 是否删除 |
| `pt` | 分区日期 |

</details>

### 2.3 关键字段统计
- **id**: 非空值1个，唯一值1个
- **cust_no**: 非空值1个，唯一值1个
- **is_delete**: 非空值1个，唯一值1个
- **create_time**: 非空值1个，唯一值1个
- **update_time**: 非空值1个，唯一值1个

## 三、数据结果分析

### 3.1 删除标志 (is_delete)
- **0 (未删除)**: 1条 (100.00%)
- **数据完整性**: 100%，无删除记录

### 3.2 客户覆盖
- **唯一客户数**: 1个
- **客户编号**: 800001054335

## 四、表关联关系

### 4.1 关联字段
- **cust_no**: 关联到其他表的客户编号字段

### 4.2 关联关系
- **→ 授信申请表**: 通过`cust_no`关联
- **→ 用信申请表**: 通过`cust_no`关联
- **→ 资产申请进件表**: 通过`cust_no`关联
- **→ 借据信息表**: 通过`cust_no`关联
- **→ 额度记录表**: 通过`cust_no`关联
- **→ 还款计划表**: 通过`cust_no`关联

### 4.3 关联分析
- 账户标签表存储客户的标签信息
- 标签信息用于客户画像和风险识别
- 与其他业务表通过客户编号关联

## 五、业务作用

1. **客户画像**: 存储客户的各类标签，用于客户分类和画像构建
2. **风险识别**: 标签值可能包含风险等级、信用模型等信息
3. **业务规则**: 标签可用于业务规则引擎的决策依据

## 六、数据质量评估

- ✅ 数据完整性良好
- ✅ 关键字段无缺失
- ⚠️ 数据量较小（仅1条），建议扩大样本量

---

**报告生成时间**: 2025-12-17

