# CDC分批输出功能 - 快速参考

## 一句话总结

将四个板块的输出从"全量+sample200"改为"每个文件500条的分批输出"

---

## 快速修改指南

### 1. 打开Jupyter
```bash
cd CDC
jupyter notebook
```

### 2. 找到修改位置
搜索: `WRITE_SAMPLE_200`

### 3. 配置部分修改

**删除这3行**:
```python
WRITE_SAMPLE_200 = True
features_out_path = OUTPUT_DIR / "xxx.csv"
features_sample200_path = OUTPUT_DIR / "xxx_sample200.csv"
```

**替换为2行**:
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500
```

### 4. 输出逻辑修改

**删除**: 全量输出 + sample200输出（约20行）

**替换为**: 分批输出循环（约30行）

详细代码见: `CDC/分批输出修改代码模板.md`

---

## 四个板块的文件前缀

| 板块 | 文件前缀 | 输出目录变量 |
|------|----------|--------------|
| 第一板块 | `cdc1_features` | `OUTPUT_DIR` |
| 第二板块 | `cdc2_features` | `OUTPUT_DIR` |
| 第三板块 | `cdc3_features` | `OUTPUT_DIR` |
| BOSS板块 | `cdcboss_features` | `output_dir` |

---

## 输出文件示例

### 修改前
```
cdc1_features_consultas.csv          # 全量，可能几百MB
cdc1_features_consultas_sample200.csv  # 前200条
```

### 修改后
```
cdc1_features_batch001_1-500.csv      # 第1批
cdc1_features_batch002_501-1000.csv   # 第2批
cdc1_features_batch003_1001-1500.csv  # 第3批
...
```

---

## 验证命令

### 查看输出文件
```bash
ls -lh CDC/outputs/*_batch*.csv
```

### 统计文件数
```bash
ls CDC/outputs/cdc1_features_batch*.csv | wc -l
```

### 检查行数
```bash
wc -l CDC/outputs/cdc1_features_batch*.csv
```

---

## 关键代码片段

### 分批输出核心逻辑
```python
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    batch_filename = f"cdc1_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
```

---

## 常见问题

### Q: 为什么不用脚本自动修改？
A: Jupyter Notebook是JSON格式，手动修改更安全可靠

### Q: 可以改成每个文件1000条吗？
A: 可以，修改 `BATCH_SIZE = 1000` 即可

### Q: 如何验证数据完整性？
A: 合并所有批次文件，检查总行数是否正确

---

## 相关文档

- 详细说明: `CDC/docs/分批输出/分批输出功能说明.md`
- 代码模板: `CDC/分批输出修改代码模板.md`
- 完成报告: `CDC/docs/分批输出/前200条记录输出功能完成报告.md`

---

**修改标识**: zlf update  
**每个文件行数**: 500条  
**文件命名**: `{前缀}_batch{批次号:03d}_{起始行}-{结束行}.csv`

