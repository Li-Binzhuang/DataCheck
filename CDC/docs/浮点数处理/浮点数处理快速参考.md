# 浮点数精度处理 - 快速参考

## 问题
BOSS板块输出CSV时没有控制浮点数精度，可能导致特征值显示不完整。

## 解决方案
在CSV输出前添加 `.round(6)` 处理，统一保留6位小数。

## 修改位置
**文件**：`CDC/BOSS板块衍生.ipynb`  
**位置**：CSV输出部分（约2180-2215行）

## 修改代码
```python
# zlf update: 对数值特征列保留6位小数
_features_to_write = features_df.copy()
_round_cols = [c for c in _features_to_write.columns if c not in {"apply_id", "request_time"}]
_features_to_write[_round_cols] = _features_to_write[_round_cols].apply(pd.to_numeric, errors="coerce").round(6)
_features_to_write.to_csv(csv_path, index=False, encoding="utf-8-sig")
```

## 处理规则
- ✅ 数值特征：保留6位小数
- ✅ apply_id：不处理（保持原样）
- ✅ request_time：不处理（保持原样）
- ✅ 全量数据和sample200：都使用相同处理

## 四个板块状态
| 板块 | 状态 | 说明 |
|------|------|------|
| 第一板块 | ✅ 已有 | 已有round(6)处理 |
| 第二板块 | ✅ 已有 | 已有round(6)处理 |
| 第三板块 | ✅ 已有 | 已有round(6)处理 |
| BOSS板块 | ✅ 已修复 | 本次添加round(6)处理 |

## 验证命令
```bash
# 检查所有板块的round(6)使用
grep -n "round(6)" CDC/*板块衍生.ipynb

# 查看BOSS板块的zlf update注释
grep -n "zlf update" CDC/BOSS板块衍生.ipynb
```

## 输出文件
- `CDC/outputs/cdcboss_features_full_data.csv` - 全量数据（6位小数）
- `CDC/outputs/cdcboss_features_sample200.csv` - 样本数据（6位小数）

## 下一步
1. 重新运行 `BOSS板块衍生.ipynb`
2. 检查输出文件中的数值精度
3. 确认特征值显示完整（如 `cdc_boss_num_pagos_mean_607`）

## 相关文档
- 详细说明：`CDC/浮点数处理功能说明.md`
- 空值填充：`CDC/zlf_update_summary.md`
