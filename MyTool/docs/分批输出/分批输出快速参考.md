# CDC板块衍生脚本 - 分批输出快速参考

## 📋 修改目标
将输出从"全量文件+sample200"改为"分批输出"，每个文件500条数据

## 🔧 修改步骤

### 1. 修改配置（所有板块）
```python
# 删除这行
WRITE_SAMPLE_200 = True  # ❌ 删除

# 添加这行
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # ✅ 新增
```

### 2. 修改输出逻辑

#### 删除的代码
```python
# 删除单文件输出
_features_to_write.to_csv(features_out_path, ...)

# 删除sample200输出
if WRITE_SAMPLE_200:
    _features_sample200 = _features_to_write.head(200)
    _features_sample200.to_csv(...)
```

#### 添加的代码
```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    # 文件名格式：{前缀}_batch{批次号}_{起始行}-{结束行}.csv
    batch_filename = f"{前缀}_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
```

## 📁 输出文件前缀

| 板块 | 前缀 | 示例文件名 |
|------|------|-----------|
| 第一板块 | `cdc1_features` | `cdc1_features_batch001_1-500.csv` |
| 第二板块 | `cdc2_features` | `cdc2_features_batch001_1-500.csv` |
| 第三板块 | `cdc3_features` | `cdc3_features_batch001_1-500.csv` |
| BOSS板块 | `cdcboss_features` | `cdcboss_features_batch001_1-500.csv` |

## 📊 输出示例

假设有1500行数据：
```
cdc1_features_batch001_1-500.csv      # 500行
cdc1_features_batch002_501-1000.csv   # 500行
cdc1_features_batch003_1001-1500.csv  # 500行
```

## 🔍 验证命令

```bash
# 查看输出文件
ls -lh CDC/outputs/*_batch*.csv

# 统计文件数量
ls CDC/outputs/*_batch*.csv | wc -l

# 检查文件行数
wc -l CDC/outputs/cdc1_features_batch*.csv
```

## ⚙️ 配置调整

```python
BATCH_SIZE = 100   # 每个文件100行
BATCH_SIZE = 500   # 每个文件500行（推荐）
BATCH_SIZE = 1000  # 每个文件1000行
```

## ✅ 优点

- ✅ 文件大小可控
- ✅ 方便分批查看
- ✅ 便于数据核对
- ✅ 文件名包含行范围

## 📚 详细文档

- `分批输出功能说明.md` - 完整修改说明
- `add_sample200_output.py` - 通用模板

---

**修改时间**：2026-01-28  
**修改标识**：zlf update
