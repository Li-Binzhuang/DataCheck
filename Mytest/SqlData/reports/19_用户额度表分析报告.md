# 用户额度表分析报告

## 一、表基本信息

**表名**: `ods_mx_aprv_cust_credit_limit_df` (用户额度表)
**表代码**: ods_mx_aprv_cust_credit_limit_df
**总记录数**: 124条
**字段数**: 21个

## 二、表结构分析

### 2.1 个人分析

**表的作用定位**：
用户额度表用于存储用户的授信额度信息，包括总额度、可用额度、已用额度等关键信息。
    （待补充详细分析）

### 2.2 核心字段

- `id`: 核心标识字段
- `cust_no`: 核心标识字段
- `encrypt_id_no`: 核心标识字段
- `hash_id_no`: 核心标识字段
<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | id |
| `app_system` | app_system |
| `cust_no` | cust_no |
| `id_no` | id_no |
| `encrypt_id_no` | encrypt_id_no |
| `hash_id_no` | hash_id_no |
| `total_limit` | total_limit |
| `pre_use_limit` | pre_use_limit |
| `available_limit` | available_limit |
| `total_use_limit` | total_use_limit |
| `use_limit` | use_limit |
| `expire_date` | expire_date |
| `status` | status |
| `remark` | remark |
| `is_delete` | is_delete |
| `version` | version |
| `create_time` | create_time |
| `created_by` | created_by |
| `update_time` | update_time |
| `updated_by` | updated_by |
| `pt` | pt |

</details>

### 2.3 关键字段统计

- **id**: 非空值124个，唯一值1个
- **app_system**: 非空值124个，唯一值1个
- **cust_no**: 非空值124个，唯一值1个
- **encrypt_id_no**: 非空值124个，唯一值1个
- **hash_id_no**: 非空值124个，唯一值1个
- **total_limit**: 非空值124个，唯一值9个
- **pre_use_limit**: 非空值124个，唯一值1个
- **available_limit**: 非空值124个，唯一值25个
- **total_use_limit**: 非空值124个，唯一值11个
- **use_limit**: 非空值124个，唯一值27个

## 三、数据结果分析

### 3.1 删除标志 (is_delete)

- **0 (未删除)**: 124条 (100.00%)
- **数据完整性**: 100%，所有记录都有删除标志

### 3.2 客户覆盖

- **唯一客户数**: 1个
- **客户编号示例**: 800001054335

## 四、表关联关系

### 4.1 关联字段

- **cust_no**: 关联到其他表的cust_no字段

### 4.2 关联关系

（待补充关联关系分析）

### 4.3 关联分析

（待补充关联分析）

## 五、业务作用

1. **数据存储**: 存储相关业务数据
2. **业务支持**: 支持相关业务功能
（待补充详细业务作用）

## 六、数据质量评估

- ✅ 数据完整性良好，大部分字段有值
- ⚠️ 存在2个完全空字段
- ✅ 数据量充足（124条）

---

**报告生成时间**: 2026-01-19

