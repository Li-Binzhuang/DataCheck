# 规则执行记录表分析报告

## 一、表基本信息

**表名**: `ods_mx_aprv_approve_rule_record_df` (规则执行记录表)  
**表代码**: ods_mx_aprv_approve_rule_record_df  
**总记录数**: 52条  
**字段数**: 26个

## 二、表结构分析

### 2.1 个人分析

**表的作用定位**：
规则执行记录表记录规则引擎的执行历史，包括请求参数、响应参数、执行状态等。

### 2.2 核心字段
- `id`: 主键ID
- `apply_id`: 申请ID（关联字段）
- `rule_config_id`: 规则配置ID
- `rule_type`: 规则类型
- `execute_step`: 执行步骤
- `status`: 状态
- `request_param`: 请求参数
- `response_param`: 响应参数
- `audit_status`: 审核状态
- `cust_no`: 客户编号
- `error_count`: 错误次数
- `error_desc`: 错误描述
- `business_type`: 业务类型

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `apply_id` | 申请ID |
| `rule_config_id` | 规则配置ID |
| `sub_package` | 子包 |
| `rule_type` | 规则类型 |
| `execute_step` | 执行步骤 |
| `execute_step_sort` | 执行步骤排序 |
| `status` | 状态 |
| `request_param` | 请求参数 |
| `response_param` | 响应参数 |
| `remark` | 备注 |
| `is_delete` | 是否删除 |
| `version` | 版本号 |
| `create_time` | 创建时间 |
| `created_by` | 创建人 |
| `update_time` | 更新时间 |
| `updated_by` | 更新人 |
| `audit_status` | 审核状态 |
| `cust_no` | 客户编号 |
| `app_system` | 应用系统 |
| `error_count` | 错误次数 |
| `error_desc` | 错误描述 |
| `allinone_request_param` | 全量请求参数 |
| `business_type` | 业务类型 |
| `pt` | 分区日期 |

</details>

<details>
<summary>📋 查看全部字段和字段含义</summary>

| 字段名 | 字段含义 |
|--------|----------|
| `id` | 主键ID |
| `apply_id` | 申请ID |
| `rule_config_id` | 规则配置ID |
| `sub_package` | 子包 |
| `rule_type` | 规则类型 |
| `execute_step` | 执行步骤 |
| `execute_step_sort` | 执行步骤排序 |
| `status` | 状态 |
| `request_param` | 请求参数 |
| `response_param` | 响应参数 |
| `remark` | 备注 |
| `audit_status` | 审核状态 |
| `cust_no` | 客户编号 |
| `app_system` | 应用系统 |
| `error_count` | 错误次数 |
| `error_desc` | 错误描述 |
| `allinone_request_param` | 全量请求参数 |
| `business_type` | 业务类型 |

</details>

### 2.3 关键字段统计
- **id**: 非空值52个，唯一值52个
- **apply_id**: 非空值52个，唯一值多个
- **rule_type**: 非空值52个，唯一值4个
- **status**: 非空值52个，唯一值2个
- **cust_no**: 非空值52个，唯一值12个

## 三、数据结果分析

### 3.1 状态字段 (Status) 分析
- **400 (Success 成功)**: 36条 (69.23%)
- **300 (Fail 失败)**: 16条 (30.77%)

**结论**: 规则执行成功率约69%，需要关注失败原因。

### 3.2 规则类型 (rule_type) 分析
- **INCREASE_LIMIT**: 26条 (50.00%)
- **USE_CREDIT_APPLY**: 10条 (19.23%)
- **CREDIT_APPLY**: 8条 (15.38%)
- **OPERATION_INCREASE_LIMIT**: 8条 (15.38%)

**结论**: 额度增加规则执行最多，占50%。

### 3.3 错误分析
- **error_count**: 需要进一步统计
- **error_desc**: 需要进一步统计

## 四、表关联关系

### 4.1 关联字段
- **apply_id**: 关联到授信申请表的id

### 4.2 上游关联（规则执行记录表 ← 授信申请表）

#### 4.2.1 授信申请表 → 规则执行记录表
- **关联字段**: `id` → `apply_id`
- **关联率**: 100.00% (8/8)
- **关联详情**:
  - 授信申请总数: 8
  - 规则执行记录总数: 52
  - 规则执行记录关联的授信申请数: 8
  - 平均每个授信申请的规则执行记录数: 6.5条 (52/8)
- **业务含义**: 每个授信申请会触发多个规则执行

### 4.3 下游关联（规则执行记录表 → 其他表）
- 无直接下游表，规则执行记录是规则引擎的执行日志

## 五、业务流程位置

```
授信申请（提交）
  ↓
规则执行记录表（本表：规则引擎执行）
  ↓
授信申请（审核结果）
```

## 六、关键业务指标

### 6.1 规则执行成功率
- **规则执行成功率**: 69.23% (36/52)
- **规则执行失败率**: 30.77% (16/52)

### 6.2 规则类型分布
- **额度增加规则**: 50.00%
- **用信申请规则**: 19.23%
- **授信申请规则**: 15.38%
- **操作额度增加规则**: 15.38%

### 6.3 规则执行频率
- **平均每个授信申请的规则执行数**: 6.5条
- **最多规则执行数**: 需要进一步统计

## 七、数据质量评估

- ✅ 关联关系清晰，100%关联到授信申请
- ✅ 关键字段完整
- ⚠️ 规则执行成功率需要提升（当前69.23%）
- ⚠️ 需要分析失败原因

## 八、业务建议

1. **规则优化**: 分析30.77%失败的原因，优化规则配置
2. **规则执行效率**: 分析规则执行的时间效率
3. **规则类型优化**: 根据业务需求调整规则类型分布

---

**报告生成时间**: 2025-12-17






