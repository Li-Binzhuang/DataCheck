# CDC分批输出功能 - 手动修改指南

## 🎯 目标

将四个板块的输出从"全量+sample200"改为"每个文件500条的分批输出"

---

## 📝 修改步骤（每个板块都一样）

### 第1步：打开 Jupyter Notebook

```bash
cd CDC
jupyter notebook
```

### 第2步：找到输出配置部分

在 Notebook 中搜索：`WRITE_SAMPLE_200`

你会看到类似这样的代码：

```python
WRITE_OUTPUTS = True
WRITE_FLAT_CSV = False  # 已经改好了
WRITE_SAMPLE_200 = True  # ← 找到这行
```

### 第3步：修改配置（2行改1行）

**删除这2行**：
```python
WRITE_SAMPLE_200 = True
features_out_path = OUTPUT_DIR / "cdc1_features_consultas.csv"
features_sample200_path = OUTPUT_DIR / "cdc1_features_consultas_sample200.csv"
```

**替换为1行**：
```python
BATCH_SIZE = 500  # zlf update: 改为分批输出，每个文件500条数据
```

### 第4步：修改输出逻辑

找到这段代码（在配置下面约20行）：

```python
_features_to_write.to_csv(
    features_out_path,
    index=False,
    encoding="utf-8-sig",
)

if WRITE_SAMPLE_200:
    _features_sample200 = _features_to_write.head(200)
    _features_sample200.to_csv(
        features_sample200_path,
        index=False,
        encoding="utf-8-sig",
    )

print("written:")
if WRITE_FLAT_CSV:
    print("-", consultas_out_path)
print("-", features_out_path)
if WRITE_SAMPLE_200:
    print("-", features_sample200_path)
```

**全部删除**，替换为下面的代码 👇

---

## 📋 四个板块的替换代码

### 第一板块（cdc1_features）

```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    batch_filename = f"cdc1_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", consultas_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

### 第二板块（cdc2_features）

```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    batch_filename = f"cdc2_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", creditos_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

### 第三板块（cdc3_features）

```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    batch_filename = f"cdc3_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", flat_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

### BOSS板块（cdcboss_features）

找到这段代码：

```python
if WRITE_FEATURES_CSV:
    csv_filename = f"cdcboss_features_full_data.csv"
    csv_path = Path("outputs") / csv_filename
    csv_path.parent.mkdir(parents=True, exist_ok=True)
    
    _features_to_write = features_df.copy()
    _round_cols = [c for c in _features_to_write.columns if c not in {"apply_id", "request_time"}]
    _features_to_write[_round_cols] = _features_to_write[_round_cols].apply(pd.to_numeric, errors="coerce").round(6)
    
    _features_to_write.to_csv(csv_path, index=False, encoding="utf-8-sig")
    
    if WRITE_SAMPLE_200:
        csv_sample200_filename = "cdcboss_features_sample200.csv"
        csv_sample200_path = Path("outputs") / csv_sample200_filename
        features_sample200 = _features_to_write.head(200)
        features_sample200.to_csv(csv_sample200_path, index=False, encoding="utf-8-sig")
        print(f"[WRITE] BOSS 特征前200条样本已输出到: {csv_sample200_path.resolve()}")
        print(f"[INFO] 样本数据形状: {features_sample200.shape}")
    
    print(f"[WRITE] BOSS 特征全量数据已输出到: {csv_path.resolve()}")
    print(f"[INFO] 输出数据形状: {_features_to_write.shape}")
    print(f"[INFO] 包含 {_features_to_write.shape[0]} 行数据，{_features_to_write.shape[1]} 列特征（含 apply_id）")
else:
    print("[INFO] WRITE_FEATURES_CSV=False，跳过 CSV 输出")
```

**全部替换为**：

```python
if WRITE_FEATURES_CSV:
    from pathlib import Path
    
    output_dir = Path("outputs")
    output_dir.mkdir(parents=True, exist_ok=True)
    
    # zlf update: 对数值特征列保留6位小数
    _features_to_write = features_df.copy()
    _round_cols = [c for c in _features_to_write.columns if c not in {"apply_id", "request_time"}]
    _features_to_write[_round_cols] = _features_to_write[_round_cols].apply(pd.to_numeric, errors="coerce").round(6)
    
    # zlf update: 分批输出功能 - 每个文件500条数据
    total_rows = len(_features_to_write)
    num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE
    
    print("[INFO] 开始分批输出特征文件")
    print(f"[INFO] 总数据量: {total_rows} 行")
    print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
    print(f"[INFO] 输出文件数: {num_batches} 个")
    print()
    
    batch_files = []
    
    for batch_idx in range(num_batches):
        start_idx = batch_idx * BATCH_SIZE
        end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
        
        batch_data = _features_to_write.iloc[start_idx:end_idx]
        
        batch_filename = f"cdcboss_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
        batch_path = output_dir / batch_filename
        
        batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
        batch_files.append(batch_path)
        
        print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
        print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")
    
    print()
    print("[SUCCESS] 分批输出完成！")
    print("written:")
    for batch_file in batch_files:
        print("-", batch_file)
else:
    print("[INFO] WRITE_FEATURES_CSV = False，跳过特征文件输出")
```

---

## ✅ 修改检查清单

每个板块修改完成后，检查：

- [ ] 删除了 `WRITE_SAMPLE_200 = True`
- [ ] 添加了 `BATCH_SIZE = 500`
- [ ] 删除了全量输出代码
- [ ] 删除了 sample200 输出代码
- [ ] 添加了分批输出循环
- [ ] 文件前缀正确（cdc1/cdc2/cdc3/cdcboss）
- [ ] 运行 Notebook 无报错

---

## 🎯 修改顺序建议

1. **第一板块** - 先修改这个，测试效果
2. **BOSS板块** - 数据量大，最需要分批
3. **第二板块** - 中等优先级
4. **第三板块** - 中等优先级

---

## 🔍 验证方法

修改完成后，运行 Notebook，检查输出：

```bash
# 查看输出文件
ls -lh CDC/outputs/*_batch*.csv

# 应该看到类似这样的文件：
# cdc1_features_batch001_1-500.csv
# cdc1_features_batch002_501-1000.csv
# ...
```

---

## 💡 小技巧

1. **复制粘贴** - 直接复制上面的代码，粘贴到 Jupyter 中
2. **注意缩进** - Python 对缩进敏感，保持一致
3. **保存备份** - 修改前先保存一个备份
4. **逐个测试** - 修改一个，测试一个

---

## ❓ 遇到问题？

如果遇到问题，随时告诉我：
- 报错信息
- 在哪个板块
- 修改到哪一步

我会帮你解决！

---

**修改标识**: zlf update  
**每个文件行数**: 500条  
**文件命名**: `{前缀}_batch{批次号:03d}_{起始行}-{结束行}.csv`

