# 只对比共同特征功能 - 实现总结

## 📋 修改概述

**修改日期：** 2026-03-13
**功能名称：** 只对比共同特征
**修改类型：** 新增功能

## 🎯 需求说明

在CSV数据对比模块中，增加一个新的配置选项"只对比共同特征"，允许用户选择只对比两个文件中都存在的特征列，自动忽略单边存在的列。

### 使用场景
- file1中的`pt`列（映射后为`local_all_sms_pt_v3`）在file2中不存在
- 希望只对比共同列，不报告缺失列

## 📝 修改内容

### 1. 前端界面修改

#### 文件：`templates/index.html`

**修改位置：** 第371-420行（特征名称映射配置区域下方）

**新增内容：**
```html
<!-- 只对比共同特征配置 -->
<div class="form-group">
    <div class="checkbox-group">
        <input type="checkbox" id="compare-common-features-only">
        <label for="compare-common-features-only" style="margin: 0;">只对比共同特征</label>
    </div>
    <p style="color: #666; font-size: 10px; margin-top: 3px; margin-left: 22px; line-height: 1.4;">
        勾选后，只对比两个文件中都存在的特征列，自动忽略单边存在的列
    </p>
</div>
```

### 2. 前端脚本修改

#### 文件：`static/js/data-compare.js`

**修改1：配置保存函数（第375-440行）**
```javascript
// 在 saveCompareConfig() 函数中添加
compare_common_features_only: document.getElementById('compare-common-features-only').checked,
```

**修改2：配置加载函数（第442-640行）**
```javascript
// 在 loadCompareConfig() 函数中添加
if (scenario.compare_common_features_only !== undefined) {
    document.getElementById('compare-common-features-only').checked = scenario.compare_common_features_only;
    console.log('[DEBUG] 设置只对比共同特征:', scenario.compare_common_features_only);
}
```

**修改3：执行对比函数（第180-250行）**
```javascript
// 在 executeCompare() 函数的 config 对象中添加
compare_common_features_only: document.getElementById('compare-common-features-only').checked,
```

### 3. 后端路由修改

#### 文件：`web/routes/compare_routes.py`

**修改位置：** 第34-150行（execute_compare_flow函数）

**修改内容：**
```python
# 在调用 compare_two_files_func 时添加新参数
comparison_results = compare_two_files_func(
    file1_path,
    file2_path,
    config['key_column_1'],
    config['key_column_2'],
    config['feature_start_1'],
    config['feature_start_2'],
    config.get('convert_feature_to_number', True),
    config.get('ignore_default_fill', False),
    config.get('enable_column_mapping', False),
    config.get('mapping_file', None),
    config.get('mapping_prefix', ''),
    config.get('mapping_suffix', ''),
    config.get('ignore_zero_nan', False),
    config.get('enable_tolerance', False),
    config.get('tolerance_value', 0.000001),
    config.get('compare_common_features_only', False)  # 新增参数
)
```

### 4. 核心对比逻辑修改

#### 文件：`data_comparison/job/data_comparator.py`

**修改1：函数签名（第54-70行）**
```python
def compare_two_files(
    sql_file_path: str,
    api_file_path: str,
    sql_key_column,
    api_key_column,
    sql_feature_start: int = 1,
    api_feature_start: int = 1,
    convert_feature_to_number: bool = True,
    ignore_default_fill: bool = False,
    enable_column_mapping: bool = False,
    mapping_file: str = None,
    mapping_prefix: str = "",
    mapping_suffix: str = "",
    ignore_zero_nan: bool = False,
    enable_tolerance: bool = False,
    tolerance_value: float = 0.000001,
    compare_common_features_only: bool = False  # 新增参数
):
```

**修改2：文档字符串（第71-100行）**
```python
"""
Args:
    ...
    compare_common_features_only: 是否只对比共同特征（默认False，对比所有特征）
    
Returns:
    包含对比结果的字典
"""
```

**修改3：日志输出（第115-125行）**
```python
print(f"只对比共同特征: {compare_common_features_only}")
```

**修改4：特征列筛选逻辑（第180-220行）**
```python
# 筛选共同特征（如果启用）
if compare_common_features_only:
    print(f"\n[特征列筛选] 只对比共同特征: 启用")
    
    # 计算两个文件特征列的交集
    feature_set_sql = set(feature_cols_sql)
    feature_set_api = set(feature_cols_api)
    common_features = feature_set_sql & feature_set_api
    
    # 找出独有的列
    sql_only_features = feature_set_sql - feature_set_api
    api_only_features = feature_set_api - feature_set_sql
    
    print(f"  file1特征列数: {len(feature_cols_sql)}")
    print(f"  file2特征列数: {len(feature_cols_api)}")
    print(f"  共同特征列数: {len(common_features)}")
    
    if sql_only_features:
        print(f"  file1独有列 ({len(sql_only_features)}个): {', '.join(sorted(list(sql_only_features))[:5])}" + 
              (f" ... (还有{len(sql_only_features)-5}个)" if len(sql_only_features) > 5 else ""))
    else:
        print(f"  file1独有列: (无)")
    
    if api_only_features:
        print(f"  file2独有列 ({len(api_only_features)}个): {', '.join(sorted(list(api_only_features))[:5])}" + 
              (f" ... (还有{len(api_only_features)-5}个)" if len(api_only_features) > 5 else ""))
    else:
        print(f"  file2独有列: (无)")
    
    if not common_features:
        raise ValueError("错误: 两个文件没有共同的特征列，无法进行对比！请检查特征名称映射配置。")
    
    # 只保留共同的特征列（保持原有顺序）
    feature_cols_sql = [col for col in feature_cols_sql if col in common_features]
    feature_cols_api = [col for col in feature_cols_api if col in common_features]
    
    print(f"  将只对比 {len(common_features)} 个共同特征列")
else:
    print(f"\n[特征列筛选] 只对比共同特征: 未启用，将对比所有特征列")
```

### 5. 执行脚本修改

#### 文件：`data_comparison/execute_data_comparison.py`

**修改1：读取配置（第80-95行）**
```python
compare_common_features_only = scenario.get('compare_common_features_only', False)
```

**修改2：日志输出（第115-125行）**
```python
print(f"  只对比共同特征: {compare_common_features_only}")
```

**修改3：调用函数（第122-140行）**
```python
comparison_results = compare_two_files(
    sql_file_path,
    api_file_path,
    sql_key_column,
    api_key_column,
    sql_feature_start,
    api_feature_start,
    convert_feature_to_number,
    ignore_default_fill,
    enable_column_mapping,
    mapping_file,
    mapping_prefix,
    mapping_suffix,
    ignore_zero_nan,
    False,  # enable_tolerance
    0.000001,  # tolerance_value
    compare_common_features_only  # 新增参数
)
```

## 📄 新增文档

1. **只对比共同特征功能说明.md** - 完整功能说明文档
2. **只对比共同特征快速参考.md** - 快速参考指南
3. **只对比共同特征功能实现总结.md** - 本文档

## ✅ 功能特性

1. **默认不启用**：保持现有逻辑，向后兼容
2. **灵活配置**：用户可根据需要选择是否启用
3. **详细日志**：输出被排除的列信息
4. **配置可保存**：配置会保存到`data_comparison/config.json`
5. **错误处理**：如果没有共同列会抛出明确的错误提示
6. **与映射配合**：可以和特征名称映射功能完美配合使用

## 🔄 执行逻辑

```
1. 读取文件
   ↓
2. 根据特征起始列索引提取特征列
   ↓
3. 应用特征名称映射（如果启用）
   ↓
4. 筛选共同特征（如果启用"只对比共同特征"）
   - 计算交集：common_features = set(file1_cols) & set(file2_cols)
   - 只保留共同列
   - 输出被排除的列
   ↓
5. 执行数据对比
```

## 🧪 测试建议

### 测试场景1：基本功能测试
- file1: `id`, `age`, `income`, `pt`, `score`
- file2: `id`, `age`, `income`, `score`
- 启用"只对比共同特征"
- 预期：只对比3个共同列，排除`pt`

### 测试场景2：结合映射测试
- file1: `age`, `income`, `pt`, `score`
- file2: `local_all_sms_age_v3`, `local_all_sms_income_v3`, `local_all_sms_score_v3`
- 启用映射：file1添加前缀`local_all_sms_`后缀`_v3`
- 启用"只对比共同特征"
- 预期：只对比3个共同列，排除`local_all_sms_pt_v3`

### 测试场景3：无共同列测试
- file1: `age`, `income`
- file2: `score`, `pt`
- 启用"只对比共同特征"
- 预期：抛出错误"两个文件没有共同的特征列"

### 测试场景4：配置保存加载测试
- 配置并勾选"只对比共同特征"
- 保存配置
- 刷新页面
- 加载配置
- 预期：复选框状态正确恢复

## 📊 影响范围

### 修改的文件
1. `templates/index.html` - 前端UI
2. `static/js/data-compare.js` - 前端逻辑
3. `web/routes/compare_routes.py` - 后端路由
4. `data_comparison/job/data_comparator.py` - 核心对比逻辑
5. `data_comparison/execute_data_comparison.py` - 执行脚本

### 不受影响的部分
- 主键列处理逻辑
- 特征起始列索引逻辑
- 特征名称映射逻辑
- 报告生成逻辑
- 其他对比选项（忽略默认填充值、忽略0和NaN、容错对比等）

## ✅ 验证清单

- [x] 前端UI正确显示复选框
- [x] 配置可以正确保存
- [x] 配置可以正确加载
- [x] 后端正确接收参数
- [x] 核心逻辑正确筛选共同列
- [x] 日志输出详细信息
- [x] 无共同列时正确报错
- [x] 与特征名称映射配合正常
- [x] 代码无语法错误
- [x] 文档完整

## 🎉 完成状态

✅ 所有代码修改已完成
✅ 所有文档已创建
✅ 代码语法检查通过
✅ 功能逻辑验证完成

## 📝 使用示例

### Web界面配置
```
☑ 启用特征名称映射
  映射文件: file1
  前缀: local_all_sms_
  后缀: _v3
☑ 只对比共同特征
```

### 配置文件示例
```json
{
  "scenarios": [{
    "name": "当前配置",
    "enabled": true,
    "sql_file": "file1.csv",
    "api_file": "file2.csv",
    "sql_key_column": [0],
    "api_key_column": [0],
    "sql_feature_start": 1,
    "api_feature_start": 1,
    "enable_column_mapping": true,
    "mapping_file": "file1",
    "mapping_prefix": "local_all_sms_",
    "mapping_suffix": "_v3",
    "compare_common_features_only": true
  }]
}
```

## 🔗 相关文档

- 功能说明：`只对比共同特征功能说明.md`
- 快速参考：`只对比共同特征快速参考.md`
- 特征名称映射：`特征名称映射功能说明.md`
