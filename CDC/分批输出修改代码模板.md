# CDC板块衍生脚本 - 分批输出修改代码模板

## 使用说明

本文档提供四个板块的具体修改代码，可以直接复制到Jupyter Notebook中使用。

---

## 第一板块衍生.ipynb

### 1. 修改配置部分（约1400行）

**删除这些行**：
```python
WRITE_SAMPLE_200 = True  # True：输出前200条记录的小文件；False：跳过样本输出

# 你最终只需要 3 个 outputs：
# 1) 原始数据展开后的明细（consultas_flat.csv）
# 2) 衍生后的特征表（cdc1_features_consultas.csv）
# 3) 前200条记录的小文件（cdc1_features_consultas_sample200.csv）- 用于快速查看和测试

consultas_out_path = OUTPUT_DIR / "consultas_flat.csv"
features_out_path = OUTPUT_DIR / "cdc1_features_consultas.csv"
features_sample200_path = OUTPUT_DIR / "cdc1_features_consultas_sample200.csv"
```

**替换为**：
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # 每个文件的行数，可以根据需要调整

# 你最终输出：
# 1) 原始数据展开后的明细（consultas_flat.csv）
# 2) 衍生后的特征表 - 分批输出多个文件（cdc1_features_batch001_1-500.csv, batch002_501-1000.csv, ...）

consultas_out_path = OUTPUT_DIR / "consultas_flat.csv"
```

### 2. 修改输出逻辑部分（约1420-1445行）

**删除这些行**：
```python
_features_to_write.to_csv(
    features_out_path,
    index=False,
    encoding="utf-8-sig",
)

# 输出前200条记录的小文件（用于快速查看和测试）
if WRITE_SAMPLE_200:
    _features_sample200 = _features_to_write.head(200)
    _features_sample200.to_csv(
        features_sample200_path,
        index=False,
        encoding="utf-8-sig",
    )

print("written:")
if WRITE_FLAT_CSV:
    print("-", consultas_out_path)
print("-", features_out_path)
if WRITE_SAMPLE_200:
    print("-", features_sample200_path)
```

**替换为**：
```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE  # 向上取整

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []  # 记录所有输出的文件路径

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    # 提取当前批次的数据
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    # 生成文件名：cdc1_features_batch{批次号}_{起始行}-{结束行}.csv
    batch_filename = f"cdc1_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    # 输出到CSV
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", consultas_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

---

## 第二板块衍生.ipynb

### 1. 修改配置部分（约1595行）

**删除**：
```python
WRITE_SAMPLE_200 = True  # True：输出前200条记录的小文件；False：跳过样本输出
```

**替换为**：
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # 每个文件的行数，可以根据需要调整
```

### 2. 修改输出逻辑部分（约1610-1641行）

**删除全量输出和sample200部分，替换为**：
```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    # 生成文件名：cdc2_features_batch{批次号}_{起始行}-{结束行}.csv
    batch_filename = f"cdc2_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", creditos_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

---

## 第三板块衍生.ipynb

### 1. 修改配置部分（约915行）

**删除**：
```python
WRITE_SAMPLE_200 = True  # True：输出前200条记录的小文件；False：跳过样本输出
```

**替换为**：
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # 每个文件的行数，可以根据需要调整
```

### 2. 修改输出逻辑部分（约930-956行）

**删除全量输出和sample200部分，替换为**：
```python
# zlf update: 分批输出功能 - 每个文件500条数据
total_rows = len(_features_to_write)
num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE

print("[INFO] 开始分批输出特征文件")
print(f"[INFO] 总数据量: {total_rows} 行")
print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
print(f"[INFO] 输出文件数: {num_batches} 个")
print()

batch_files = []

for batch_idx in range(num_batches):
    start_idx = batch_idx * BATCH_SIZE
    end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
    
    batch_data = _features_to_write.iloc[start_idx:end_idx]
    
    # 生成文件名：cdc3_features_batch{批次号}_{起始行}-{结束行}.csv
    batch_filename = f"cdc3_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
    batch_path = OUTPUT_DIR / batch_filename
    
    batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
    batch_files.append(batch_path)
    
    print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
    print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")

print()
print("[SUCCESS] 分批输出完成！")
print("written:")
if WRITE_FLAT_CSV:
    print("-", flat_out_path)
for batch_file in batch_files:
    print("-", batch_file)
```

---

## BOSS板块衍生.ipynb

### 1. 修改配置部分（约2234行）

**删除**：
```python
WRITE_SAMPLE_200 = True  # 设置为 True 输出前200条样本，False 则跳过
```

**替换为**：
```python
# zlf update: 改为分批输出，每个文件500条数据
BATCH_SIZE = 500  # 每个文件的行数，可以根据需要调整
```

### 2. 修改输出逻辑部分（约2237-2270行）

**删除整个 if WRITE_FEATURES_CSV 块，替换为**：
```python
if WRITE_FEATURES_CSV:
    from pathlib import Path
    
    output_dir = Path("outputs")
    output_dir.mkdir(parents=True, exist_ok=True)
    
    # zlf update: 对数值特征列保留6位小数
    _features_to_write = features_df.copy()
    _round_cols = [c for c in _features_to_write.columns if c not in {"apply_id", "request_time"}]
    _features_to_write[_round_cols] = _features_to_write[_round_cols].apply(pd.to_numeric, errors="coerce").round(6)
    
    # zlf update: 分批输出功能 - 每个文件500条数据
    total_rows = len(_features_to_write)
    num_batches = (total_rows + BATCH_SIZE - 1) // BATCH_SIZE
    
    print("[INFO] 开始分批输出特征文件")
    print(f"[INFO] 总数据量: {total_rows} 行")
    print(f"[INFO] 批次大小: {BATCH_SIZE} 行/文件")
    print(f"[INFO] 输出文件数: {num_batches} 个")
    print()
    
    batch_files = []
    
    for batch_idx in range(num_batches):
        start_idx = batch_idx * BATCH_SIZE
        end_idx = min((batch_idx + 1) * BATCH_SIZE, total_rows)
        
        batch_data = _features_to_write.iloc[start_idx:end_idx]
        
        # 生成文件名：cdcboss_features_batch{批次号}_{起始行}-{结束行}.csv
        batch_filename = f"cdcboss_features_batch{batch_idx + 1:03d}_{start_idx + 1}-{end_idx}.csv"
        batch_path = output_dir / batch_filename
        
        batch_data.to_csv(batch_path, index=False, encoding="utf-8-sig")
        batch_files.append(batch_path)
        
        print(f"[WRITE] 批次 {batch_idx + 1}/{num_batches}: {batch_filename}")
        print(f"        行范围: {start_idx + 1} - {end_idx} ({len(batch_data)} 行)")
    
    print()
    print("[SUCCESS] 分批输出完成！")
    print("written:")
    for batch_file in batch_files:
        print("-", batch_file)
else:
    print("[INFO] WRITE_FEATURES_CSV = False，跳过特征文件输出")
```

---

## 修改注意事项

### 1. 保持缩进一致
- 确保代码缩进与原有代码一致
- Python对缩进非常敏感

### 2. 变量名对应
- 第一板块：`OUTPUT_DIR`, `consultas_out_path`
- 第二板块：`OUTPUT_DIR`, `creditos_out_path`
- 第三板块：`OUTPUT_DIR`, `flat_out_path`
- BOSS板块：`output_dir`（注意小写）

### 3. 文件前缀
- 第一板块：`cdc1_features`
- 第二板块：`cdc2_features`
- 第三板块：`cdc3_features`
- BOSS板块：`cdcboss_features`

### 4. 测试建议
- 修改后先用小数据集测试
- 检查输出文件数量和行数
- 验证数据完整性

---

## 验证方法

### 1. 检查输出文件
```bash
# 查看输出文件
ls -lh CDC/outputs/*_batch*.csv

# 统计文件数量
ls CDC/outputs/*_batch*.csv | wc -l
```

### 2. 检查文件行数
```bash
# 检查每个文件的行数（包含表头）
wc -l CDC/outputs/cdc1_features_batch*.csv
```

### 3. 验证数据完整性
```python
import pandas as pd
from pathlib import Path

# 读取所有批次文件
batch_files = sorted(Path("CDC/outputs").glob("cdc1_features_batch*.csv"))

# 合并所有批次
dfs = [pd.read_csv(f) for f in batch_files]
merged_df = pd.concat(dfs, ignore_index=True)

print(f"批次文件数: {len(batch_files)}")
print(f"合并后总行数: {len(merged_df)}")
print(f"apply_id 唯一值: {merged_df['apply_id'].nunique()}")
```

---

**创建时间**：2026-01-28  
**修改标识**：zlf update  
**修改类型**：输出优化 - 分批输出
