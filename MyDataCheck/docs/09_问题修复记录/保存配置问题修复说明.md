# 保存配置问题修复说明

## 问题描述

在Web界面点击"💾 保存配置"按钮时报错：
```
name 'data_comparison_dir' is not defined
```

---

## 问题原因

**变量名不一致**：
- `web/config.py` 中定义的是大写变量：`DATA_COMPARISON_DIR`、`DATA_COMPARISON_JOB_DIR`
- `web/routes/compare_routes.py` 中使用的是小写变量：`data_comparison_dir`、`data_comparison_job_dir`
- 导致运行时找不到变量定义

---

## 修复方案

### 修改文件
`web/routes/compare_routes.py`

### 修改内容

#### 修改前
```python
from web.config import (
    SCRIPT_DIR, JOB_DIR, ONLINE_JOB_DIR, DATA_COMPARISON_JOB_DIR, COMMON_DIR,
    API_OUTPUT_DIR, ONLINE_OUTPUT_DIR, COMPARE_OUTPUT_DIR,
    API_INPUT_DIR, ONLINE_INPUT_DIR, COMPARE_INPUT_DIR
)
from web.utils import OutputCapture

compare_bp = Blueprint('compare_routes', __name__)
```

#### 修改后
```python
from web.config import (
    SCRIPT_DIR, JOB_DIR, ONLINE_JOB_DIR, DATA_COMPARISON_JOB_DIR, COMMON_DIR,
    API_OUTPUT_DIR, ONLINE_OUTPUT_DIR, COMPARE_OUTPUT_DIR,
    API_INPUT_DIR, ONLINE_INPUT_DIR, COMPARE_INPUT_DIR, DATA_COMPARISON_DIR
)
from web.utils import OutputCapture

compare_bp = Blueprint('compare_routes', __name__)

# 为了兼容性，创建小写别名
data_comparison_dir = DATA_COMPARISON_DIR
data_comparison_job_dir = DATA_COMPARISON_JOB_DIR
```

---

## 修复步骤

1. **修改代码**
   - 在导入语句中添加 `DATA_COMPARISON_DIR`
   - 创建小写别名变量

2. **重启服务**
   ```bash
   cd MyDataCheck
   ./stop_web.sh
   ./start_web.sh
   ```

3. **测试功能**
   - 访问 http://localhost:5001
   - 进入"数据对比"页面
   - 点击"💾 保存配置"按钮
   - 确认显示"配置保存成功"

---

## 验证结果

### 测试命令
```bash
curl -X POST http://localhost:5001/api/compare/config/save \
  -H "Content-Type: application/json" \
  -d '{
    "scenarios": [{
      "name": "测试配置",
      "enabled": true,
      "description": "测试",
      "sql_file": "test1.csv",
      "api_file": "test2.csv",
      "sql_key_column": 0,
      "api_key_column": 0,
      "sql_feature_start": 1,
      "api_feature_start": 1,
      "convert_feature_to_number": true,
      "output_prefix": "test"
    }],
    "global_config": {
      "default_convert_feature_to_number": true,
      "default_sql_key_column": 0,
      "default_api_key_column": 0,
      "default_sql_feature_start": 1,
      "default_api_feature_start": 1
    }
  }'
```

### 预期响应
```json
{
  "message": "配置保存成功",
  "success": true
}
```

### 配置文件
配置保存在：`data_comparison/config.json`

---

## 影响范围

### 修复的功能
- ✅ 数据对比配置保存
- ✅ 数据对比配置加载

### 不受影响的功能
- ✅ 接口数据对比
- ✅ 线上灰度落数对比
- ✅ PKL文件解析
- ✅ 数据对比执行

---

## 相关文件

| 文件 | 说明 |
|------|------|
| `web/routes/compare_routes.py` | 数据对比路由（已修复） |
| `web/config.py` | 配置定义文件 |
| `data_comparison/config.json` | 配置保存文件 |
| `data_comparison/job/config_manager.py` | 配置管理模块 |

---

## 预防措施

### 代码规范
1. **统一变量命名**：所有配置变量使用大写
2. **导入检查**：确保导入所有需要的变量
3. **测试覆盖**：添加配置保存/加载的测试用例

### 开发建议
```python
# 推荐：直接使用大写变量
from web.config import DATA_COMPARISON_DIR
config_path = os.path.join(DATA_COMPARISON_DIR, "config.json")

# 或者：创建别名（兼容旧代码）
from web.config import DATA_COMPARISON_DIR as data_comparison_dir
config_path = os.path.join(data_comparison_dir, "config.json")
```

---

## 测试清单

- [x] 保存配置功能
- [x] 加载配置功能
- [x] 配置文件生成
- [x] 服务重启后功能正常
- [x] Web界面操作正常

---

**修复时间**：2026-01-27  
**修复状态**：✅ 已完成  
**测试状态**：✅ 通过

---

## 快速测试

### 启动服务
```bash
cd MyDataCheck
./start_web.sh
```

### 访问界面
```
http://localhost:5001
```

### 测试步骤
1. 点击"📊 数据对比"
2. 上传两个CSV文件
3. 配置参数
4. 点击"💾 保存配置"
5. 确认显示"配置保存成功！"

---

**问题已解决！现在可以正常保存配置了。** ✅
