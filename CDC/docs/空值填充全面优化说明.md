# CDC衍生脚本空值填充全面优化说明

## 📋 检查结果汇总

运行检查脚本后发现以下问题：

| 板块 | fillna(0) | fillna(np.nan) | = np.nan | = 0 | fillna(-999) |
|------|-----------|----------------|----------|-----|--------------|
| 第一板块 | 3 | 0 | 0 | 6 | 14 ✅ |
| 第二板块 | 4 | 0 | 2 | 0 | 6 ✅ |
| 第三板块 | 4 | 0 | 0 | 0 | 5 ✅ |
| BOSS板块 | 1 | 1 | 0 | 8 | 16 ✅ |
| **总计** | **12** | **1** | **2** | **14** | **41** ✅ |

**需要修复**: 15 处（12 + 1 + 2）

## 🔍 详细问题分析

### 1. fillna(0) 问题（12处）

这些地方使用了 `fillna(0)`，应该改为 `fillna(-999)`：

#### 第一板块（3处）

1. **行 429**: cnt 特征填充
   ```python
   # 修改前
   cnt = cnt.fillna(0).astype(int)
   
   # 修改后
   cnt = cnt.fillna(-999).astype(int)
   ```

2. **行 512**: tipo_credito cnt 特征填充
   ```python
   # 修改前
   cnt_t = gt.size().unstack().reindex(...).fillna(0).astype(int)
   
   # 修改后
   cnt_t = gt.size().unstack().reindex(...).fillna(-999).astype(int)
   ```

3. **行 1213**: unique_cnt 特征填充
   ```python
   # 修改前
   out["unique_cnt"] = out["unique_cnt"].fillna(0).astype(int)
   
   # 修改后
   out["unique_cnt"] = out["unique_cnt"].fillna(-999).astype(int)
   ```

#### 第二板块（4处）

1. **行 777**: total_cnt 特征填充
   ```python
   # 修改前
   total_cnt = sub.groupby(...).size().reindex(...).fillna(0).astype("int32")
   
   # 修改后
   total_cnt = sub.groupby(...).size().reindex(...).fillna(-999).astype("int32")
   ```

2. **行 789**: cnt 特征填充
   ```python
   # 修改前
   cnt = cnt.fillna(0).astype("int32")
   
   # 修改后
   cnt = cnt.fillna(-999).astype("int32")
   ```

3. **行 827**: cnt_local 特征填充
   ```python
   # 修改前
   cnt_local = cnt_local.fillna(0).astype("int32")
   
   # 修改后
   cnt_local = cnt_local.fillna(-999).astype("int32")
   ```

4. **行 1658**: unique_cnt 特征填充
   ```python
   # 修改前
   out["unique_cnt"] = out["unique_cnt"].fillna(0).astype(int)
   
   # 修改后
   out["unique_cnt"] = out["unique_cnt"].fillna(-999).astype(int)
   ```

#### 第三板块（4处）

1. **行 255**: total cnt 特征填充
   ```python
   # 修改前
   total = sub.groupby(...).size().reindex(...).fillna(0).astype(int)
   
   # 修改后
   total = sub.groupby(...).size().reindex(...).fillna(-999).astype(int)
   ```

2. **行 266**: prev_cnt 特征填充
   ```python
   # 修改前
   prev_cnt = prev_cnt.fillna(0).astype(int)
   
   # 修改后
   prev_cnt = prev_cnt.fillna(-999).astype(int)
   ```

3. **行 286**: resp_cnt 特征填充
   ```python
   # 修改前
   resp_cnt = resp_cnt.fillna(0).astype(int)
   
   # 修改后
   resp_cnt = resp_cnt.fillna(-999).astype(int)
   ```

4. **行 879**: unique_cnt 特征填充
   ```python
   # 修改前
   out["unique_cnt"] = out["unique_cnt"].fillna(0).astype(int)
   
   # 修改后
   out["unique_cnt"] = out["unique_cnt"].fillna(-999).astype(int)
   ```

#### BOSS板块（1处）

1. **行 2025**: unique_cnt 特征填充
   ```python
   # 修改前
   out["unique_cnt"] = out["unique_cnt"].fillna(0).astype(int)
   
   # 修改后
   out["unique_cnt"] = out["unique_cnt"].fillna(-999).astype(int)
   ```

### 2. fillna(np.nan) 问题（1处）

#### BOSS板块（1处）

**行 688**: zero_balance_cnt 计算
```python
# 修改前
zero_balance_cnt = int((cre["saldoActual"].fillna(np.nan) == 0).sum())

# 修改后
zero_balance_cnt = int((cre["saldoActual"].fillna(-999) == 0).sum())
```

### 3. = np.nan 问题（2处）

#### 第二板块（2处）

这两处是**数据清洗逻辑**，用于标记不合理的数据：

1. **行 590**: 最后还款日期早于开户日期
   ```python
   # 当前代码（数据清洗）
   creditos_df.loc[creditos_df["days_pay_since_open"] < 0, "days_pay_since_open"] = np.nan
   
   # 建议：保持不变，这是数据质量检查
   # 后续会通过 fillna(-999) 处理
   ```

2. **行 602**: 最严重逾期日期早于开户日期
   ```python
   # 当前代码（数据清洗）
   creditos_df.loc[creditos_df["days_worst_arrears_since_open"] < 0, "days_worst_arrears_since_open"] = np.nan
   
   # 建议：保持不变，这是数据质量检查
   # 后续会通过 fillna(-999) 处理
   ```

**说明**: 这两处不需要修改，因为：
- 目的是标记不合理的数据（日期逻辑错误）
- 后续会通过 `fillna(-999)` 统一处理
- 保持数据清洗逻辑的清晰性

### 4. = 0 问题（14处）

这些地方直接赋值为 0，需要检查是否应该为 -999：

#### 第一板块（6处）

所有这些都是在**没有数据**的情况下赋值，应该改为 -999：

```python
# 修改前
out[f"consultas_{window_days}d_{gid}_cnt"] = 0
out[f"consultas_{window_days}d_{gid}_ratio"] = 0.0
out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = 0.0
out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = 0
out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = 0.0
out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = 0.0

# 修改后
out[f"consultas_{window_days}d_{gid}_cnt"] = -999
out[f"consultas_{window_days}d_{gid}_ratio"] = -999
out[f"consultas_{window_days}d_{gid}_notnull_ratio"] = -999
out[f"consultas_{window_days}d_tipo_{tid}_cnt"] = -999
out[f"consultas_{window_days}d_tipo_{tid}_ratio"] = -999
out[f"consultas_{window_days}d_tipo_{tid}_notnull_ratio"] = -999
```

#### BOSS板块（8处）

这些都是在**没有数据**的情况下赋值，应该改为 -999：

```python
# 修改前
out[w_prefix + "high_util_cnt"] = 0.0
out["boss_auto_loan_query_cnt"] = 0.0
out["boss_credit_card_query_cnt"] = 0.0
out[f"boss_w{w}d_query_amount_sum"] = 0.0
out["boss_has_verified_employment_flag"] = 0.0
out["boss_salary_verified_flag"] = 0.0
out["boss_company_nunique"] = 0.0
out["boss_recent_move_flag_180d"] = 0.0

# 修改后
out[w_prefix + "high_util_cnt"] = -999
out["boss_auto_loan_query_cnt"] = -999
out["boss_credit_card_query_cnt"] = -999
out[f"boss_w{w}d_query_amount_sum"] = -999
out["boss_has_verified_employment_flag"] = -999
out["boss_salary_verified_flag"] = -999
out["boss_company_nunique"] = -999
out["boss_recent_move_flag_180d"] = -999
```

## 📝 修复清单

### 必须修复（13处）

| 板块 | 类型 | 数量 | 优先级 |
|------|------|------|--------|
| 第一板块 | fillna(0) | 3 | 🔴 高 |
| 第一板块 | = 0 | 6 | 🔴 高 |
| 第二板块 | fillna(0) | 4 | 🔴 高 |
| 第三板块 | fillna(0) | 4 | 🔴 高 |
| BOSS板块 | fillna(0) | 1 | 🔴 高 |
| BOSS板块 | fillna(np.nan) | 1 | 🔴 高 |
| BOSS板块 | = 0 | 8 | 🔴 高 |

### 建议保持（2处）

| 板块 | 类型 | 数量 | 说明 |
|------|------|------|------|
| 第二板块 | = np.nan | 2 | 🟢 数据清洗逻辑，保持不变 |

## 🛠️ 修复步骤

### 步骤1: 备份文件

```bash
cp CDC/第一板块衍生.ipynb CDC/第一板块衍生.ipynb.backup
cp CDC/第二板块衍生.ipynb CDC/第二板块衍生.ipynb.backup
cp CDC/第三板块衍生.ipynb CDC/第三板块衍生.ipynb.backup
cp CDC/BOSS板块衍生.ipynb CDC/BOSS板块衍生.ipynb.backup
```

### 步骤2: 使用查找替换

在 Jupyter Notebook 或文本编辑器中：

1. **替换 fillna(0) → fillna(-999)**
   - 查找: `.fillna(0)`
   - 替换: `.fillna(-999)`
   - 注意: 只替换 cnt 相关的特征

2. **替换 fillna(np.nan) → fillna(-999)**
   - 查找: `.fillna(np.nan)`
   - 替换: `.fillna(-999)`

3. **替换 = 0 → = -999**
   - 手动检查每一处
   - 确认是空值情况再替换

### 步骤3: 添加注释

在每处修改后添加注释：

```python
# zlf update: 空值填充为-999
cnt = cnt.fillna(-999).astype(int)
```

### 步骤4: 验证修复

运行检查脚本：

```bash
python CDC/scripts/check_and_fix_fillna.py
```

期望结果：
- fillna(0): 0 处
- fillna(np.nan): 0 处
- = np.nan: 2 处（数据清洗，正常）
- fillna(-999): 54 处（41 + 13）

## 📊 修复前后对比

### 修复前

```python
# 第一板块
cnt = cnt.fillna(0).astype(int)  # ❌ 空值填充为0
out[f"consultas_{window_days}d_{gid}_cnt"] = 0  # ❌ 无数据时为0

# 第二板块
total_cnt = sub.groupby(...).fillna(0)  # ❌ 空值填充为0

# BOSS板块
zero_balance_cnt = int((cre["saldoActual"].fillna(np.nan) == 0).sum())  # ❌ 使用np.nan
out["boss_auto_loan_query_cnt"] = 0.0  # ❌ 无数据时为0
```

### 修复后

```python
# 第一板块
# zlf update: 空值填充为-999
cnt = cnt.fillna(-999).astype(int)  # ✅ 空值填充为-999
# zlf update: 空值填充为-999
out[f"consultas_{window_days}d_{gid}_cnt"] = -999  # ✅ 无数据时为-999

# 第二板块
# zlf update: 空值填充为-999
total_cnt = sub.groupby(...).fillna(-999)  # ✅ 空值填充为-999

# BOSS板块
# zlf update: 空值填充为-999
zero_balance_cnt = int((cre["saldoActual"].fillna(-999) == 0).sum())  # ✅ 使用-999
# zlf update: 空值填充为-999
out["boss_auto_loan_query_cnt"] = -999  # ✅ 无数据时为-999
```

## ✅ 验证方法

### 方法1: 运行检查脚本

```bash
python CDC/scripts/check_and_fix_fillna.py
```

### 方法2: 检查输出文件

```python
import pandas as pd

# 读取输出文件
df = pd.read_csv('CDC/outputs/cdc1_features_consultas.csv')

# 检查是否有0值（cnt特征不应该有0）
cnt_cols = [col for col in df.columns if '_cnt' in col]
for col in cnt_cols:
    zero_count = (df[col] == 0).sum()
    if zero_count > 0:
        print(f"⚠️  {col}: 有 {zero_count} 个0值")

# 检查是否有NaN
nan_count = df.isnull().sum().sum()
print(f"NaN数量: {nan_count}")  # 应该是0

# 检查-999的数量
minus999_count = (df == -999).sum().sum()
print(f"-999数量: {minus999_count}")
```

## 📈 预期效果

修复后，所有特征的空值处理将统一为 -999：

| 特征类型 | 修复前 | 修复后 |
|---------|--------|--------|
| cnt 特征 | 0 或 NaN | -999 |
| sumsq 特征 | 0 或 NaN | -999 |
| cnt_in_Ndays 特征 | 0 或 NaN | -999 |
| ratio 特征 | 0 或 NaN | -999 |
| 其他数值特征 | NaN | -999 |

## 🎯 总结

### 需要修复的问题

1. ✅ fillna(0) → fillna(-999)：12 处
2. ✅ fillna(np.nan) → fillna(-999)：1 处
3. ✅ = 0 → = -999：14 处（无数据情况）
4. 🟢 = np.nan：2 处（数据清洗，保持不变）

### 修复后的状态

- 所有 cnt 特征空值：-999
- 所有 sumsq 特征空值：-999
- 所有 cnt_in_Ndays 特征空值：-999
- 所有数值特征空值：-999
- 数据清洗逻辑：保持不变

---

**创建时间**: 2025-01-29  
**检查工具**: `CDC/scripts/check_and_fix_fillna.py`  
**需要修复**: 27 处（13 必须 + 14 建议）  
**预计工作量**: 30-60 分钟
